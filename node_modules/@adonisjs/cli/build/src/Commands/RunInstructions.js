"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
const ace_1 = require("@adonisjs/ace");
const standalone_1 = require("@adonisjs/application/build/standalone");
const sink_1 = require("@adonisjs/sink");
const helpers_1 = require("../Services/helpers");
class RunInstructions extends ace_1.BaseCommand {
    constructor() {
        super(...arguments);
        this.projectRoot = process.cwd();
    }
    async handle() {
        const rcContents = await helpers_1.getRcContents(this.projectRoot);
        if (!rcContents) {
            this.$error('Make sure your project root has .adonisrc.json file to continue');
            return;
        }
        try {
            const app = new standalone_1.Application(this.projectRoot, {}, rcContents, {});
            await sink_1.executeInstructions(this.projectName, this.projectRoot, app);
        }
        catch (error) {
            this.$error(this.colors.red(`Unable to execute instructions for ${this.projectName} package`));
            this.$info(`Sink version: ${sink_1.sinkVersion}`);
            console.log('');
            console.log(this.colors.bgRed(' Error stack  '));
            console.log(error.stack.split('\n').map((line) => {
                return `  ${this.colors.red(line)}`;
            }).join('\n'));
        }
    }
}
RunInstructions.commandName = 'run:instructions';
RunInstructions.description = 'Run instructions for a given adonisjs package.';
__decorate([
    ace_1.args.string({
        description: 'Name of the package. It must be installed as your project dependency',
        name: 'projectName',
    }),
    __metadata("design:type", String)
], RunInstructions.prototype, "projectName", void 0);
exports.default = RunInstructions;
