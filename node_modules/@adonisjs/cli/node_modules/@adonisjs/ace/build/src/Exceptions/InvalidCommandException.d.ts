import { Exception } from '@poppinss/utils';
import { SerializedCommand } from '../Contracts';
/**
 * Raised when command is not registered with kernel
 */
export declare class InvalidCommandException extends Exception {
    commandName: string;
    suggestions: SerializedCommand[];
    static invoke(commandName: string, suggestions: SerializedCommand[]): InvalidCommandException;
}
