/// <reference types="node" />
import { EventEmitter } from 'events';
import { TracerContract } from '../Contracts';
/**
 * Tracer is used to emit event from the IoC container
 * at different steps. Read the guides to understand
 * how tracer works
 */
export declare class Tracer extends EventEmitter implements TracerContract {
    private namespaces;
    in(namespace: string, cached: boolean): void;
    out(): void;
}
export default function tracer(enabled: boolean): Tracer;
