/**
 * Renders the instructions.md markdown file. Ignores any errors occurred during the
 * process.
 */
export declare function renderMarkdown(mdFilePath: string, moduleName: string): Promise<void>;
